{{ form_start(form) }}
<div class="row">
    <div class="col-xs-3 col-md-6 col-lg-4">{{ form_row(form.artist) }}</div>
    <div class="col-md-4">{{ form_row(form.style) }}</div>
    <div class="col-md-4">{{ form_row(form.filename) }}</div>
</div>

<button class="btn btn-primary btn-sm">{{ button|default('Enregistrer') }}</button>
<a href="{{ path('admin_groupes_index') }}" class="btn btn-secondary btn-sm">Retour</a>
{{ form_end(form) }}